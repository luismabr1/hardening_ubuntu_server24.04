# Original variables (unchanged unless noted)
FW_ADMIN='127.0.0.1'
SSH_GRPS='sudo soporte'
SSH_PORT='2123'
SYSCTL_CONF='./misc/sysctl.conf'
AUDITD_MODE='1'
AUDITD_RULES='./misc/audit-base.rules ./misc/audit-aggressive.rules ./misc/audit-docker.rules'
LOGROTATE_CONF='./misc/logrotate.conf'
NTPSERVERPOOL='0.ubuntu.pool.ntp.org 1.ubuntu.pool.ntp.org 2.ubuntu.pool.ntp.org 3.ubuntu.pool.ntp.org pool.ntp.org'
TIMEDATECTL=''
VERBOSE='Y'
AUTOFILL='N'
ADMINEMAIL='root@localhost'
KEEP_SNAPD='Y'  # Kept as Y per original, no change needed
CHANGEME='me se olvido cambiarlo'      # Add something just to verify that you actually glanced the code
LXC='0'          # Added default value '0' (not in LXC) since it was used conditionally in scripts; original had no default

# Configuration files (unchanged)
ADDUSER='/etc/adduser.conf'
AUDITDCONF='/etc/audit/auditd.conf'
AUDITRULES='/etc/audit/rules.d/hardening.rules'
COMMONPASSWD='/etc/pam.d/common-password'
COMMONACCOUNT='/etc/pam.d/common-account'
COMMONAUTH='/etc/pam.d/common-auth'
COREDUMPCONF='/etc/systemd/coredump.conf'
DEFAULTGRUB='/etc/default/grub.d'
DISABLEFS='/etc/modprobe.d/disablefs.conf'
DISABLEMOD='/etc/modprobe.d/disablemod.conf'
DISABLENET='/etc/modprobe.d/disablenet.conf'
FAILLOCKCONF='/etc/security/faillock.conf'
JOURNALDCONF='/etc/systemd/journald.conf'
LIMITSCONF='/etc/security/limits.conf'
LOGINDCONF='/etc/systemd/logind.conf'
LOGINDEFS='/etc/login.defs'
LOGROTATE='/etc/logrotate.conf'
PAMLOGIN='/etc/pam.d/login'
PSADCONF='/etc/psad/psad.conf'
PSADDL='/etc/psad/auto_dl'
RESOLVEDCONF='/etc/systemd/resolved.conf'
RKHUNTERCONF='/etc/default/rkhunter'
RSYSLOGCONF='/etc/rsyslog.conf'
SECURITYACCESS='/etc/security/access.conf'
SSHFILE='/etc/ssh/ssh_config'
SSHDFILE='/etc/ssh/sshd_config'
SYSCTL='/etc/sysctl.conf'
SYSTEMCONF='/etc/systemd/system.conf'
TIMESYNCD='/etc/systemd/timesyncd.conf'
UFWDEFAULT='/etc/default/ufw'
USERADD='/etc/default/useradd'
USERCONF='/etc/systemd/user.conf'

# New variables added during script enhancements
# These were introduced to support logging, backups, and package management across functions
LOG_FILE='/var/log/user_hardening.log'  # Added for consistent logging across all scripts
BACKUP_DIR='/root/backups'              # Added for storing backups of modified config files
TIMESTAMP=$(date +%Y%m%d_%H%M%S)        # Added for timestamped backups
SCRIPT_COUNT=0                          # Added to track script execution steps
APT='apt-get'                           # Added to standardize package manager command
SECUREBOOT_DB='/usr/share/secureboot/db'  # Added for Secure Boot key management
POSTFIX_CONF='/etc/postfix/main.cf'      # Added for Postfix configuration
LSB_RELEASE='/usr/bin/lsb_release'  # Path to lsb_release command, added for verification and consistency
PSAD_SERVICE='psad.service'  # Defines the psad service name for consistency and verification
REBOOT_REQUIRED='/var/run/reboot-required' # Path to reboot-required file, added for consistency and reference
NSSWITCH_CONF='/etc/nsswitch.conf'  # Path to nsswitch.conf, added for DNS resolution order configuration
HOSTS_EQUIV='/etc/hosts.equiv'  # Path to system-wide hosts.equiv file, added for consistency and reference
SECURETTY='/etc/securetty'  # Path to securetty file, added for restricting root login terminals
DEBUG_SHELL_SERVICE='debug-shell.service'  # Defines debug-shell service name for consistency
SSH_MODULI='/etc/ssh/moduli'  # Path to SSH moduli file, added for secure key filtering in f_sshdconfig
SSH_SERVICE='ssh.service'  # Defines SSH service name, added for restart in f_sshdconfig
SUDOERS_DIR='/etc/sudoers.d'  # Directory for sudoers configuration files, added for f_sudo
PAM_SU='/etc/pam.d/su'  # Path to PAM configuration for su, added for f_sudo
SUDO_LOG='/var/log/sudo.log'  # Path to sudo log file, added for logging in f_sudo
SUID_LIST='./misc/suid.list'  # Path to list of binaries to remove SUID bits from, added for f_suid
SHELLS_FILE='/etc/shells'  # Path to shells file, added for SUID removal in f_suid
SYSCTL_SERVICE='systemd-sysctl.service'  # Defines systemd-sysctl service name, added for f_sysctl
ARPBIN='/usr/sbin/arp'  # Path to arp binary, added for network interface detection in f_sysctl
PINGBIN='/bin/ping'  # Path to ping binary, added for latency checks in f_timesyncd
DIGBIN='/usr/bin/dig'  # Path to dig binary, added for NTP server resolution in f_timesyncd
TIMESYNCD_SERVICE='systemd-timesyncd.service'  # Defines timesyncd service name, added for f_timesyncd
UFW_SERVICE='ufw.service'  # Defines UFW service name, added for status checks in f_firewall
RC_INIT='/etc/init.d/rc'  # Path to rc init script, added for umask setting in f_umask
PROFILE='/etc/profile'  # Path to profile file, added for umask in f_umask
BASHRC='/etc/bash.bashrc'  # Path to bashrc file, added for umask in f_umask
PROFILE_D='/etc/profile.d'  # Directory for profile.d scripts, added for TMOUT in f_umask
AUTOLOGOUT_SH='/etc/profile.d/autologout.sh'  # Path to autologout script, added for TMOUT in f_umask
USBGUARD_CONF='/etc/usbguard/rules.conf'  # Path to USBGuard rules configuration file, added for f_usbguard
USBGUARD_SERVICE='usbguard.service'  # Defines USBGuard service name, added for f_usbguard
PASSWD_FILE='/etc/passwd'  # Path to passwd file, added for user verification in f_users
SHADOW_FILE='/etc/shadow'  # Path to shadow file, added for user password backup in f_users
AIDE_CONF='/etc/aide/aide.conf'  # Path to AIDE configuration file
